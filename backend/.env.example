# Local PostgreSQL connection — update with your Supabase URL when deploying
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/shuttleiq
PORT=3001
